<p id="notice"><%= notice %></p>

<p>
  <strong>Type:</strong>
  <%= @material.category %>
</p>

<p>
  <strong>Title:</strong>
  <%= @material.title %>
</p>

<p>
  <strong>File url:</strong>
  <%= link_to @material.title, "#{request.base_url}#{url_for(@material.file)}" %>
</p>

<p>
  <strong>Download file:</strong>
  <%= link_to @material.title, rails_blob_path(@material.file, disposition: 'attachment') %>
</p>

<%= link_to 'Edit', edit_material_path(@material) %> |
<%= link_to 'Back', materials_path %>
